"use client";

import PremiumLeadForm from "@/components/site/PremiumLeadForm";

/** Conversion-optimized multi-step lead form for public contractor sites. */
export default function RequestServiceForm(props) {
  return <PremiumLeadForm {...props} />;
}
